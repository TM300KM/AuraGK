<!-- theme.liquid: Estructura base del tema -->
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8">
    <title>{{ settings.theme_name }}</title>
    <link href="{{ 'styles.css' | asset_url }}" rel="stylesheet">
    <script src="{{ 'script.js' | asset_url }}" defer></script>
  </head>
  <body>
    {% section 'hero' %}
    {{ content_for_layout }}
  </body>
</html>
